
	<% if user_signed_in? %>
	  <div class="alert alert-success">Welcome, <%= current_user.email %>. 
	  <%= link_to 'Logged In [click to logout]', destroy_user_session_path, :method => :delete %></div>
	<% else %>
	  <div class="alert alert-danger">You are not signed in. Please enter with your account to access.</div>
	  <!--<%= link_to 'Login', new_user_session_path %>-->
	<% end %>
