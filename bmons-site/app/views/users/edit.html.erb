
<% if  current_user.id == @user.id or current_user.access_level==5 %>
	<h1>Editing User</h1>

	<%= render 'form' %>

	<%= link_to 'Show', @user %><!-- |
	<%= link_to 'Back', users_path %>-->

<% end %>